// 5. Інвестор вклав  тис. грн на 20 років під 20% річних. Визначити за допомогою циклів суму, яку він одержить (без оподаткування).
let investMan = parseFloat(prompt('введи скліьки бажаєщ вкласти'))

for (let i = 0; i < 20; i++) {
    investMan *= 1.2     
}1
alert(`через 20 років ти отримаєш ${(investMan.toFixed(2)) * 0.95} грн`)